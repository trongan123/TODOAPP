<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/book"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".MainBottomSheetActivity"
            android:exported="false" />
        <activity
            android:name=".MainKotlinActivity"
            android:exported="false" />
        <activity
            android:name=".MainLoadMostJetpackActivity"
            android:exported="false"
            android:label="@string/title_activity_main_load_most_jetpack"
            android:theme="@style/Theme.App" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.App" />
        <activity
            android:name=".MainJetpackActivity"
            android:exported="false"
            android:label="@string/title_activity_main_jetpack"
            android:theme="@style/Theme.App" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>